// place holder for now
// can be nested into folder (tabs)/map if needed
import React from "react";
import { SafeAreaView } from "react-native";

const Map = () => {
  return <SafeAreaView></SafeAreaView>;
};

export default Map;
